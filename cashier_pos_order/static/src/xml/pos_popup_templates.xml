<?xml version="1.0" encoding="UTF-8"?>
<!--    Add a popup window-->
<templates id="template" xml:space="preserve">
   <t t-name="CashierPopup" owl="1">
      <div class="popup product-line-popup">
         <main class="body">
            <div>
               <header>
                  <h3>
                     <span>Cashier</span>
                      <i class="fa fa-times" style="float:right"
                         t-on-click="cancel"/>
                      <hr/>
                  </h3>
                   <!-- <div class="product-img-create-popup">
                     <img t-att-src="imageUrl" id="img_url_tag_create"/>
                  </div> -->
                   <select  t-ref="CashierRef"
                           style="width:35%;height:30px;border-radius:5px;">
                            <!-- <t t-log="props.sales_persons"/> -->
                       <t t-foreach='employee_allowed || []'
                          t-as='employee' t-key="employee.id">
                            <!-- <t t-log="sales_person"/> -->
                            <!-- <t t-log="employee.id"/>
                            <t t-log="employee_allowed"/> -->
                           <option  t-att-value='employee.name'
                                   t-att-id="employee.id">
                                <t t-esc='employee.name'/>
                            </option>
                       </t>
                   </select>
               </header>
            </div>
         </main>
          <footer class="footer cash-move">
            <div class="button confirm disable highlight" t-on-click="confirm">
                            Confirm
                        </div>
              <div class="button cancel" t-on-click="cancel">
                    Cancel
            </div>
         </footer>
      </div>
   </t>
</templates>
